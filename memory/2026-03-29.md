# 2026-03-29

## memory_search 恢复状态复核（00:43-00:45）

- 用户先问“我昨天做了什么”；按规则先尝试调用 `memory_search` 做记忆检索
- 当前 `memory_search` 仍不可用，返回错误为 OpenAI embeddings 401，提示 API key 无效：

```text
openai embeddings failed: 401 {
  "error": {
    "message": "Incorrect API key provided: sk-3fa4d*******************************************************3899. You can find your API key at https://platform.openai.com/account/api-keys.",
    "type": "invalid_request_error",
    "code": "invalid_api_key",
    "param": null
  },
  "status": 401
}
```

- 已向用户如实说明：
  - gateway 重载 / 重启这条线已完成
  - memory_search 这条线**未恢复**，问题还没彻底收口
- 用户随后要求直接粘贴 `memory_search` 报错原文；已原样贴出
- 当前可确认结论：`memory_search` 仍在使用一个以 `sk-3fa4d...3899` 结尾的旧 key；在没有进一步修复并重新验证前，**不能说 memory_search 已恢复**
